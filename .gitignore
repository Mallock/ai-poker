node_modules
dist
.DS_Store
*.log
vitest_output.txt

# Local IDE
.vscode
.idea

# Tailwind / build caches
.cache

# Claude Code / agent tooling (not needed to build or run the app)
.claude/
.impeccable.md

# OpenSpec workflow artifacts (dev-only)
openspec/

# Dev-only smoke scripts and their captured outputs
scripts/

# Raw asset originals at repo root — the runtime copies under public/ are what the build ships.
# Leading slash anchors these to the root so we don't accidentally match public/assets/sgh_kortit/.
/character_portraits/
/sgh_kortit/

# Kenney assets are not redistributed in this repo
public/assets/kenney/*
!public/assets/kenney/README.md
!public/assets/kenney/.gitkeep
